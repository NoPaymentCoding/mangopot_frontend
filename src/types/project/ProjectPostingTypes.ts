export default interface fields {
  fieldId: number;
  fieldName: string;
}

export default interface posting {
  locName: string;
  userNickName: string;
  deadline: string;
  title: string;
  context: string;
  interest: string[];
  position: any;
}
