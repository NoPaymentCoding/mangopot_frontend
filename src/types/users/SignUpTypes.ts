export default interface interests {
    inId: number;
    name: string;
    posId: number;
    logoImg: string;
}

export default interface positions {
    id: number;
    name: string;
    logoImg: string;
}
